{
  "name": "maximedeveaux",
  "version": "1.0.0",
  "description": "Personal Website",
  "scripts": {
    "start": "parcel serve src/index.pug",
    "build": "npm run clean && parcel build --no-source-maps src/index.pug",
    "clean": "rm -rf dist",
    "watch": "parcel watch src/index.pug",
    "deploy": "npm run build && cd dist && now"
  },
  "repository": "https://github.com/dvxam/maximedeveaux",
  "author": "contact@maximedeveaux.com",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "autoprefixer": "^9.1.3",
    "babel-preset-env": "^1.7.0",
    "parcel-bundler": "^1.9.7i",
    "postcss-lh": "^2.0.2",
    "postcss-type-scale": "^1.0.0",
    "typeit": "^5.10.6",
    "webpack": "^4.17.2"
  },
  "devDependencies": {
    "pug": "^2.0.3",
    "typescript": "^3.0.3"
  }
}